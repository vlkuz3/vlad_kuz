{% extends 'base.html.twig' %}

{% block title %}CarCategory{% endblock %}

{% block body %}
    <h1>CarCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ car_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ car_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_car_category_index') }}">back to list</a>

    <a href="{{ path('app_car_category_edit', {'id': car_category.id}) }}">edit</a>

    {{ include('car_category/_delete_form.html.twig') }}
{% endblock %}
